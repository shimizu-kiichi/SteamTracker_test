add(dashboard): 管理者用ダッシュボード(Webアプリ)を追加

# スプレッドシートの文字列を取得して、Webアプリ上に表示する

最終的に、管理者用ダッシュボードでスプレッドシートを管理する。そのため、スプレッドシートのセルの文字列をGASのWebアプリで表示できるか確認する。

- `SpreadsheetApp`を使用して対象のシートにアクセス
- `getRange()`と`getValue()`を使って特定のセルの文字列を取得
- `doGet()`でテンプレートHTMLにデータを埋め込み、Webアプリ上に表示