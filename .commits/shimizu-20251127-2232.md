add(dashboard): チェック行をアーカイブへ移送

# チェック済み行をアーカイブ用シートへ移動する機能

## やること
- Apps Script 側に `completeRows` を実装し、指定行をアーカイブ用シートへコピーしてから元行を削除する
- 「アーカイブ」ボタンを `google.script.run.completeRows` と連動させ、チェックされた行番号を渡す
- ユーザー操作中はボタンをローディング状態にし、成功時はリロード・失敗時はエラー表示を行う