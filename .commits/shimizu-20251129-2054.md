add(dashboard): チェック行の削除機能を追加

# ダッシュボードから選択行を即時削除する機能

## やること
- `deleteRows`を実装し、指定行番号を管理シートから直接削除できるようにする
- 「データから削除」ボタンを押すと、チェック済み行の行番号を`google.script.run.deleteRows`に渡す
- 処理中はボタンをローディング状態にし、成功時はリロード、失敗時はエラー表示で通知する